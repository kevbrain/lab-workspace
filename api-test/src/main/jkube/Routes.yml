kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: api-test-route
  labels:
    app: api-test
spec:
  to:
    kind: Service
    name: api-test-service
    weight: 256
  port:
    targetPort: 8080
  tls:
    termination: edge
  wildcardPolicy: None
---
