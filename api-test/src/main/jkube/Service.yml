kind: Service
apiVersion: v1
metadata:
  name: api-test-service
  labels:
    app: api-test
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: api-test