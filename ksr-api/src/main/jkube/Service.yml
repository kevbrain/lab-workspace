kind: Service
apiVersion: v1
metadata:
  name: ksr-api-service
  labels:
    app: ksr-api
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: ksr-api