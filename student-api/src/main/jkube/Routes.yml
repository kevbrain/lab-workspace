kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: student-api-route
  labels:
    app: student-api
spec:
  to:
    kind: Service
    name: student-api-service
    weight: 256
  port:
    targetPort: 8080
  tls:
    termination: edge
  wildcardPolicy: None
---
