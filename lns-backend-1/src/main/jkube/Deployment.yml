kind: Deployment
apiVersion: apps/v1
metadata:
  annotations:
    build-commit: '@git.commit.id.abbrev@'
    build-date: "@git.build.time@"
  name: lns-backend-1
  labels:
    app: lns-backend-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lns-backend-1
  template:
    metadata:
      name: lns-backend-1
      labels:
        app: lns-backend-1
      annotations:
        build-commit: '@git.commit.id.abbrev@'
        build-date: "@git.build.time@"
    spec:
      volumes:
        - name: emptydirlogs
          emptyDir: {}
      containers:
        - name: microservice-pod 
          resources:
            requests:
              memory: 500m
              cpu : 750Mi
            limits:
              memory: 200m
              cpu : 250Mi
          readinessProbe:
            httpGet:
              path: actuator/health
              port: 8081
              scheme: HTTP
          env:
          ports:
            - containerPort: 8080
              protocol: TCP
              name: http
          imagePullPolicy: Always
          volumeMounts:
            - name: emptydirlogs
              mountPath: /Logs
          image: >-
            image-registry.openshift-image-registry.svc.cluster.local:5000/lab-workspace/lns-backend-1:latest
      restartPolicy: Always